- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.git:       git
    ~/.tmux.conf: tmux.conf
    ~/.vimrc:     vimrc
    ~/.zsh:       zsh
    ~/.zshrc:     zshrc
    ~/.zshenv:    zshenv

- shell:
  - [git submodule update --init --recursive, Installing submodules]

