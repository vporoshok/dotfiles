- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.profile:   profile
    ~/.tmux.conf: tmux.conf
    ~/.vimrc:     vimrc
    ~/.zsh:       zsh
    ~/.zprofile:  zprofile
    ~/.zshrc:     zshrc
    ~/.zshenv:    zshenv

- shell:
  - [git submodule update --init --recursive, Installing submodules]

